# 本地运行

完整步骤见 `系统功能与本地部署说明.md`。

```powershell
# 后端
Copy-Item .env.example backend/.env
Set-Location backend
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8000

# 新开一个终端启动前端
Set-Location frontend
npm install
npm run dev
```
