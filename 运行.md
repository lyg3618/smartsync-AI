# 1. 安装依赖
cd d:\Desktop\AI会议\smartsync\backend
pip install -r requirements.txt

# 2. 准备环境变量（复制模板并填写 MySQL 配置）
copy d:\Desktop\AI会议\smartsync\.env.example d:\Desktop\AI会议\smartsync\backend\.env
# 编辑 .env，填入你的 MYSQL_HOST / MYSQL_USER / MYSQL_PASSWORD

# 3. 启动后端
uvicorn app.main:app --reload --port 8000