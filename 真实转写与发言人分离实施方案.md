# SmartSync 真实转写 + 发言人分离（Diarization）实施方案

## 1. 目标与范围

目标：
- 将当前 `simulate_processing` 的演示逻辑替换为真实音频转写（ASR）。
- 在转写结果中标注发言人（Speaker A/B/C...）。
- 前端会议详情页按时间轴展示“谁在什么时候说了什么”。

范围：
- 支持文件上传（`mp3/wav/m4a/mp4`）后的异步处理。
- 支持中文为主、多语混说的会议场景。
- 支持 1 小时内会议在可接受时间内完成处理。

非目标（本阶段不做）：
- 真实姓名自动识别（只输出 `SPEAKER_00` 这类标签）。
- 声纹建库与跨会议身份追踪。

---

## 2. 总体方案

采用“两阶段流水线”：

1. 语音转文本（ASR）
- 引擎：`faster-whisper`（本地推理，支持中英文，速度与成本可控）。
- 输出：分段文本 + 时间戳（start/end）。

2. 发言人分离（Diarization）
- 引擎：`pyannote.audio`（基于 Hugging Face 模型）。
- 输出：说话人时间片（speaker turns）。

3. 对齐融合（Alignment）
- 将 ASR 的分段与 diarization 时间片按重叠时长对齐。
- 生成最终 transcript 段：`speaker + start_ms + end_ms + text`。

---

## 3. 技术选型建议

## 3.1 本地部署（推荐默认）

- ASR：`faster-whisper`（`large-v3` 或 `medium`）
- Diarization：`pyannote/speaker-diarization-3.1`
- 优点：数据不出本地，长期成本低，可私有化。
- 缺点：需要 GPU/较强 CPU；首次模型下载体积大。

## 3.2 云服务备选

- OpenAI / Azure / Google Speech / 阿里云语音服务（ASR）
- 第三方 diarization API 或自建 pyannote 服务
- 优点：接入快、运维轻。
- 缺点：持续成本高、数据合规要求更严格。

建议：先落地 3.1，保留接口抽象，后续可切云。

---

## 4. 后端改造设计（基于现有项目）

## 4.1 数据库变更

当前 `transcripts` 仅有：
- `meeting_id, start_ms, end_ms, text`

新增字段：
- `speaker` VARCHAR(64) NOT NULL DEFAULT 'SPEAKER_00'
- `confidence` FLOAT NULL
- `segment_no` INT NOT NULL DEFAULT 0

建议索引：
- `idx_transcripts_meeting_start (meeting_id, start_ms)`
- `idx_transcripts_meeting_speaker (meeting_id, speaker)`

## 4.2 任务状态机

在 `upload_tasks.status` 上增加细粒度状态：
- `uploading`
- `queued`
- `transcribing`
- `diarizing`
- `aligning`
- `ready_for_review`
- `failed`

并保留 `progress` 百分比更新（前端轮询不变）。

## 4.3 处理流程

上传后：
1. 保存文件到 `uploads/`
2. 后台 worker 接单
3. 音频预处理（ffmpeg：统一采样率 16k、单声道）
4. ASR 生成 `segments`
5. Diarization 生成 `speaker turns`
6. 时间对齐，写入 `transcripts`
7. 触发摘要/行动项抽取（现有 LLM 流程可复用）

失败处理：
- 记录错误码和 `error_message`
- 状态置 `failed`
- 前端展示“可重试”

---

## 5. 关键实现细节

## 5.1 音频预处理（必须）

统一格式，减少模型波动：

```bash
ffmpeg -i input.m4a -ac 1 -ar 16000 -y output.wav
```

## 5.2 ASR（faster-whisper）

建议参数：
- `model_size`: `large-v3`（GPU）/`medium`（CPU）
- `vad_filter=True`（减少静音段）
- `beam_size=5`
- `language='zh'`（或自动识别）

输出结构：
- `[{start, end, text, avg_logprob, no_speech_prob}]`

## 5.3 Diarization（pyannote）

输入：标准化 wav  
输出：`[(start, end, speaker_label)]`

注意：
- 需 `HF_TOKEN` 拉取模型。
- 首次加载较慢，建议进程内单例缓存 pipeline。

## 5.4 对齐策略

对每个 ASR segment：
1. 计算与每个 speaker turn 的时间重叠
2. 取重叠最大 speaker
3. 若重叠过小（如 < 0.2s）标记 `UNKNOWN`

可选优化：
- 邻近段平滑（防 speaker 抖动）
- 连续同 speaker 且时间相邻可合并段

---

## 6. API 与前端改造

## 6.1 API

`GET /meetings/{meeting_id}` 中 transcript 改为：

```json
{
  "id": 101,
  "start_ms": 12000,
  "end_ms": 18750,
  "speaker": "SPEAKER_01",
  "text": "这个版本我们先上线灰度。",
  "confidence": 0.92
}
```

## 6.2 前端展示

会议详情页 transcript 列表：
- 左侧 speaker tag（颜色区分）
- 中间文本
- 右侧时间戳（点击跳音频）

交互建议：
- 支持按 speaker 过滤
- 支持“连续同 speaker 折叠展示”

---

## 7. 部署与依赖

新增 Python 依赖（示例）：
- `faster-whisper`
- `pyannote.audio`
- `torch`（按 CPU/GPU 版本安装）
- `ffmpeg-python`（可选，仅封装）

系统依赖：
- `ffmpeg` 可执行文件

环境变量新增：
- `ASR_PROVIDER=local`
- `WHISPER_MODEL=large-v3`
- `DIARIZATION_PROVIDER=pyannote`
- `HF_TOKEN=...`
- `DEVICE=cuda`（或 `cpu`）

Docker 建议：
- `worker` 镜像内预装 ffmpeg
- 模型缓存目录挂载卷，避免重复下载

---

## 8. 性能与容量预估

参考（单卡 T4，`large-v3`）：
- 60 分钟音频 ASR + diarization：约 8-20 分钟（视音质与并发）

并发策略：
- 每个 worker 同时处理 1-2 条长音频任务
- 通过队列长度限流，避免 OOM

存储：
- 原始音频 + 标准化 wav + transcript JSON
- 建议引入对象存储分层归档

---

## 9. 隐私与合规

- 默认本地推理，敏感语音不出网。
- 上传文件加访问控制与过期清理策略。
- 日志不落原文，仅落 task_id 与错误摘要。
- 若启用云服务，需补充用户授权与数据处理声明。

---

## 10. 测试验收标准

功能验收：
- 上传 30 分钟中文会议音频，状态流转正确。
- transcript 每条都有 `speaker`，时间连续可跳转。
- 前端可正确显示并区分发言人。

质量验收（建议指标）：
- 中文字错误率（CER）达到可审阅水平（业务可接受）。
- 发言人分离 DER 控制在可接受区间（按场景评估）。

稳定性验收：
- 连续处理 20 条任务无 worker 崩溃。
- 失败任务可重试并有明确错误信息。

---

## 11. 分阶段落地计划

第 1 阶段（2-3 天）：
- 接入 `faster-whisper`，完成真实转写，替换 mock。

第 2 阶段（2-4 天）：
- 接入 `pyannote`，完成 speaker diarization 与对齐入库。

第 3 阶段（1-2 天）：
- 前端 speaker UI、筛选与可用性优化。

第 4 阶段（1-2 天）：
- 压测、失败重试、监控告警、上线文档。

---

## 12. 最小可行版本（MVP）定义

满足以下即视为 MVP 完成：
- 真实音频可自动转写为时间轴文本；
- 每段文本带 `speaker` 标签；
- 可在会议详情页回放并核对；
- 处理失败可见、可重试。

