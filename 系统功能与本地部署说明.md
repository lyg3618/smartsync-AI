# SmartSync 系统功能、本地部署与服务器 Docker 部署说明

> 文档版本：2026-07-19  
> 适用代码目录：`D:\Desktop\AI会议\smartsync`

本文档说明 SmartSync 当前已经实现的系统功能，以及 Windows 本地开发部署和 Linux 服务器 Docker 部署方法。文档内容以当前代码为准，不包含已经移除的本地 ASR 模型和旧消息协同配置。

## 1. 系统概述

SmartSync 是一套面向会议场景的 AI 协作系统，主要业务流程如下：

```text
录音文件、录音链接或浏览器麦克风
                  |
                  v
        阿里云听悟语音转写
                  |
                  v
       逐字稿、发言人和时间轴
                  |
                  v
     AI 摘要、决议、行动项和纪要
                  |
                  v
       人工确认、邮件分发和跟进
```

当前技术栈：

- 前端：Vue 3、Vite、Pinia、Vue Router、Element Plus
- 后端：Python 3.11、FastAPI、aiomysql、Pydantic Settings
- 数据库：MySQL 8.0
- 离线及实时转写：阿里云听悟
- 录音对象存储：Cloudflare R2，使用 S3 API 和 SigV4 预签名下载 URL
- AI 分析：OpenAI 兼容的 `/chat/completions` 接口
- 行动项分发：SMTP 邮件
- 服务器入口：Nginx

主要目录：

| 目录或文件 | 用途 |
| --- | --- |
| `frontend/` | Vue 前端应用 |
| `backend/` | FastAPI 后端和测试 |
| `backend/uploads/` | 本地运行时保存上传录音和实时 WAV 录音 |
| `nginx/` | 本地及服务器反向代理配置 |
| `docker-compose.server.yml` | 当前服务器部署使用的 Compose 文件 |
| `.env.example` | 本地环境变量模板 |
| `.env.server.example` | Docker 服务器环境变量模板 |

## 2. 当前系统功能

### 2.1 用户和权限

- 用户名、密码登录和 JWT 鉴权
- 登录状态路由保护
- 修改个人资料和密码
- 管理员与普通成员角色区分
- 管理员新增、修改和删除成员
- 管理员新增成员时初始密码为 `123456`，首次登录后应立即修改
- 会议数据按登录用户隔离

### 2.2 离线录音转写

- 支持上传 MP3、WAV、MP4 和 M4A 文件
- 前端限制单个文件最大 200 MB
- 支持导入可由公网直接访问的音视频 URL
- 本地文件先保存到 `uploads`，再通过 S3 API 上传至 Cloudflare R2
- 提交给听悟的是 R2 原生 SigV4 预签名 GET URL
- 自动轮询听悟任务状态并保存最终逐字稿
- 开启多人发言人区分，人数设置为 `0` 时由听悟自动判断
- 保存每段逐字稿的开始时间、结束时间、发言人和置信度
- 上传的原始文件保留在本地上传目录和 R2 对象存储中

重要说明：`TINGWU_S3_PUBLIC_URL_BASE` 应保持为空。当前已验证可用的方式是 R2 原生预签名 URL；把 `r2.dev` 公共地址作为听悟 `FileUrl` 可能得到 `TSC.AudioFileLink` 或 `Audio file link invalid`。

### 2.3 实时转写

- 浏览器采集麦克风音频
- 音频转换为 16 kHz、单声道、PCM S16LE 后通过 WebSocket 转发
- 页面实时展示临时识别文本和最终句段
- 自动区分发言人
- 保存最终句段的发言人和句段时间轴
- 将实际发送给听悟的 PCM 音频同步保存为 WAV 原录音
- 结束记录后自动创建会议并保存逐字稿和录音地址
- 同一用户同时只允许进行一场实时记录
- 后端支持中文、英语、粤语、日语、韩语和多语言参数；当前前端固定提交中文

实时转写当前没有启用自动章节、会议辅助、自动摘要和文本润色。SDK 虽然接收翻译事件，但页面和数据库尚未使用实时翻译结果。词级时间信息也没有单独入库，目前保存的是句段级时间轴。

### 2.4 会议管理和会议详情

- 会议列表、状态、时长和行动项数量展示
- 会议重命名和逻辑删除
- 播放原始录音
- 点击逐字稿时间点定位录音
- 查看逐字稿、发言人和时间段
- 单段修改或批量映射发言人姓名
- 编辑会议摘要、核心决议和行动项
- 确认会议内容后保存
- 查看模板纪要、行动项清单、会议摘要和决议

### 2.5 AI 分析和纪要模板

- 调用 OpenAI 兼容接口生成会议摘要、核心决议和行动项
- 为每位用户维护多个大模型连接配置
- 管理员的启用配置可作为其他用户的回退配置
- 测试大模型连接
- 新增、编辑、删除和设置默认会议纪要模板
- 按指定模板生成正式会议纪要

转写结束后系统先保存逐字稿，AI 摘要需要在会议详情中触发，并不是听悟转写任务自动生成。

### 2.6 行动项和邮件分发

- 新增、编辑、删除行动项
- 设置负责人、截止日期、状态和进度说明
- 按当前用户聚合“我的待办”
- 支持待处理、进行中和已完成状态
- 首次确认后按负责人聚合行动项并发送邮件
- 分发后修改的行动项会标记为变更项
- 支持仅再次同步有变化的行动项

当前唯一实际可用的外部分发渠道是 SMTP 邮件，不再使用旧消息协同配置。

### 2.7 搜索和消息中心

- 搜索会议、逐字稿和行动项
- 查看转写完成、转写失败、分发结果等系统通知
- 未读消息计数
- 单条标记已读和全部标记已读

## 3. 部署架构

服务器 Docker 部署拓扑如下：

```text
浏览器
  |
  | HTTP/HTTPS，实时转写同时使用 WebSocket
  v
宿主机域名和 TLS 反向代理（生产环境推荐）
  |
  v
Docker Nginx :9090
  |-- /              -> frontend:80
  |-- /api/*         -> backend:8000
  |-- /api/realtime  -> backend WebSocket
  `-- /uploads/*     -> backend 静态录音

backend
  |-- MySQL 8.0
  |-- 阿里云听悟 API/NLS
  |-- Cloudflare R2 S3 API
  |-- OpenAI 兼容 LLM
  `-- SMTP 服务
```

## 4. 部署前必须准备的外部资源

### 4.1 MySQL 数据库

当前仓库没有完整的从零建库 SQL。应用启动时只会创建少量运行时表和补充部分字段，不能从空数据库创建 `contacts`、`meetings`、`transcripts`、`action_items` 和 `upload_tasks` 等全部核心表。

因此，新服务器部署前必须先从现有可用环境导出 SmartSync 数据库结构和必要数据，再导入服务器 MySQL。仅创建一个空的 `smartsync` 数据库无法完成登录和业务操作。

仓库中的以下脚本属于增量迁移，不是完整初始化脚本：

- `backend/migrate_data_isolation.sql`
- `backend/migrate_soft_delete_meetings.sql`
- `backend/migrate_minutes_templates.sql`
- `backend/migrate_action_item_tracking.sql`
- `backend/migrate_action_item_change_tracking.sql`
- `backend/migrate_llm_connection_configs.sql`
- `backend/migrate_notifications.sql`

只对缺少相应变更的旧数据库执行这些脚本。多个 `ALTER TABLE ADD COLUMN` 脚本不是幂等脚本，重复执行可能报“字段已存在”。执行前必须备份数据库。

### 4.2 阿里云听悟

需要准备：

- 可调用听悟的 AccessKey ID
- AccessKey Secret
- 听悟项目 AppKey
- 对应账号的听悟离线转写和实时记录权限

AccessKey 和 AppKey 属于不同概念，不能用 Cloudflare R2 的访问密钥代替听悟凭据。

### 4.3 Cloudflare R2

需要准备：

- R2 账户级 S3 API Endpoint，例如 `https://ACCOUNT_ID.r2.cloudflarestorage.com`
- Bucket 名称
- 具有目标 Bucket“对象读取和写入”权限的 R2 Access Key ID 和 Secret Access Key

Endpoint 后面不要附加 Bucket 名称。例如 Bucket 为 `luying` 时，Endpoint 仍只填写账户级地址，Bucket 单独填写到 `TINGWU_S3_BUCKET`。

当前方案不使用 WebDAV，也不要求为听悟配置 `r2.dev` 公共地址。

### 4.4 大模型和 SMTP

大模型用于会议摘要、决议、行动项和模板纪要；SMTP 用于发送行动项邮件。这两项不影响基础转写，但未配置时对应功能不可用。

## 5. 环境变量

本地运行时，后端读取 `backend/.env`。Docker Compose 运行时读取项目根目录 `.env`。

推荐配置示例：

```dotenv
# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=smartsync
MYSQL_PASSWORD=REPLACE_WITH_DATABASE_PASSWORD
MYSQL_DB=smartsync

# JWT，生产环境使用至少 32 字节的随机值
SECRET_KEY=REPLACE_WITH_LONG_RANDOM_SECRET

# OpenAI 兼容大模型，可稍后在系统设置中配置
LLM_BASE_URL=https://your-llm.example.com/v1
LLM_MODEL=your-model
LLM_API_KEY=REPLACE_WITH_LLM_KEY

# 总转写开关和听悟开关
TRANSCRIPTION_ENABLED=true
TINGWU_ENABLED=true
TINGWU_ACCESS_KEY_ID=REPLACE_WITH_TINGWU_AK
TINGWU_ACCESS_KEY_SECRET=REPLACE_WITH_TINGWU_SK
TINGWU_APP_KEY=REPLACE_WITH_TINGWU_APP_KEY
TINGWU_POLL_INTERVAL_SEC=8
TINGWU_POLL_TIMEOUT_SEC=14400

# Cloudflare R2，Endpoint 不包含 Bucket
TINGWU_S3_ENDPOINT=https://ACCOUNT_ID.r2.cloudflarestorage.com
TINGWU_S3_BUCKET=REPLACE_WITH_BUCKET
TINGWU_S3_REGION=auto
TINGWU_S3_USER_AGENT=SmartSync
TINGWU_S3_PUBLIC_URL_BASE=
TINGWU_S3_PREFIX=smartsync/tingwu
TINGWU_S3_URL_EXPIRES_SEC=14400
TINGWU_S3_ACCESS_KEY_ID=REPLACE_WITH_R2_ACCESS_KEY_ID
TINGWU_S3_ACCESS_KEY_SECRET=REPLACE_WITH_R2_SECRET_ACCESS_KEY

# SMTP，可选
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-account@example.com
SMTP_PASS=REPLACE_WITH_SMTP_PASSWORD
SMTP_FROM=SmartSync <noreply@example.com>

DEBUG=false
```

配置注意事项：

- 不要把真实 `.env`、AccessKey、API Token 或数据库密码提交到 Git。
- 凭据一旦出现在截图、日志或聊天记录中，应立即撤销并重新创建。
- `TINGWU_S3_URL_EXPIRES_SEC` 建议保持 `14400`。代码会强制使用至少 3 小时的有效期。
- `TINGWU_S3_PUBLIC_URL_BASE` 留空后，系统才会生成已验证可用的 SigV4 预签名 GET URL。
- `TRANSCRIPTION_ENABLED` 和 `TINGWU_ENABLED` 必须同时为 `true` 才能使用离线及实时转写。
- `VITE_TRANSCRIPTION_ENABLED` 当前没有被前端业务代码读取，不能作为实际功能开关。

## 6. Windows 本地部署

### 6.1 环境要求

- Windows 10 或 Windows 11
- Python 3.11
- Node.js 20 和 npm
- MySQL 8.0
- Git，可选
- 能够访问阿里云听悟、Cloudflare R2 和 Python/npm 依赖源的网络

以下命令均在 PowerShell 中执行。

### 6.2 准备数据库

创建数据库：

```sql
CREATE DATABASE smartsync
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
```

然后导入已有 SmartSync 数据库备份。若数据库来自旧版本，在确认字段缺失后执行第 4.1 节列出的增量迁移。

数据库中必须至少存在一个带 `username`、`hashed_password` 和 `role` 的可登录 `contacts` 记录。迁移或导入完成后应立即修改历史默认密码。

### 6.3 配置后端

在项目根目录执行：

```powershell
Copy-Item .env.example backend/.env
notepad backend/.env
```

填写第 5 节中的配置。本地 MySQL 通常使用：

```dotenv
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
```

### 6.4 安装并启动后端

```powershell
Set-Location backend
py -3.11 -m venv .venv
Set-ExecutionPolicy -Scope Process Bypass
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

如果已经创建虚拟环境，可直接执行：

```powershell
Set-Location backend
.\.venv\Scripts\uvicorn.exe app.main:app --reload --port 8000
```

验证后端：

- 健康检查：`http://127.0.0.1:8000/health`
- API 文档：`http://127.0.0.1:8000/docs`
- 正常健康响应：`{"status":"ok"}`

### 6.5 安装并启动前端

新开一个 PowerShell 窗口：

```powershell
Set-Location frontend
npm install
npm run dev
```

当前 Vite 开发端口是 `3333`，访问：

```text
http://127.0.0.1:3333
```

前端默认把 `/api` 和 `/uploads` 代理到 `http://127.0.0.1:8000`。需要使用其他后端地址时，在 `frontend/.env.local` 中设置：

```dotenv
VITE_API_BASE_URL=/api
VITE_BACKEND_TARGET=http://127.0.0.1:8000
```

### 6.6 本地验证清单

1. 打开后端 `/health`，确认返回 200。
2. 打开前端并使用已有账号登录。
3. 上传一段短 MP3，确认任务进度最终变为“待确认”。
4. 进入会议详情，确认录音、发言人和时间轴可用。
5. 点击 AI 分析，确认摘要、决议和行动项生成成功。
6. 点击实时记录，允许麦克风权限，确认临时文本和最终句段出现。
7. 结束实时记录，进入会议详情确认 WAV 原录音可播放。

## 7. Linux 服务器 Docker 部署

### 7.1 推荐服务器条件

- Linux x86_64
- Docker Engine 24+
- Docker Compose v2
- 至少 2 核 CPU、4 GB 内存和足够的录音存储空间
- 能够访问阿里云、Cloudflare R2、Python 和 npm 依赖源
- 生产环境准备域名和 HTTPS 证书

实时麦克风采集在远程浏览器中要求安全上下文，即 HTTPS。只有 `localhost` 可以在 HTTP 下直接使用麦克风。因此服务器仅使用 `http://IP:9090` 时，离线上传可以工作，但实时记录通常会被浏览器禁止。

### 7.2 使用正确的 Compose 文件

服务器部署使用：

```text
docker-compose.server.yml
```

不要使用通用的 `docker-compose.yml` 作为当前生产入口。该文件仍包含一个 Worker 服务，但当前代码已没有对应的 `app.workers.celery_app`，启动 Worker 会失败。

### 7.3 检查 Dockerfile 中的构建代理

当前 `backend/Dockerfile` 和 `frontend/Dockerfile` 写有开发网络使用的固定代理 `172.16.31.97:8088`。如果服务器无法访问该代理，镜像构建会失败。

构建前应根据服务器网络修改或移除以下配置：

- `backend/Dockerfile` 中的 `pip config set global.proxy ...`
- `frontend/Dockerfile` 中的 `HTTP_PROXY` 和 `HTTPS_PROXY`
- 不建议在生产构建中长期保留 `npm config set strict-ssl false`

如果服务器确实需要代理，应替换为服务器可访问的代理地址，并确保代理凭据没有写入 Git。

### 7.4 上传项目并配置环境

示例目录：

```bash
sudo mkdir -p /opt/smartsync
sudo chown "$USER":"$USER" /opt/smartsync
cd /opt/smartsync
```

将代码放入该目录后：

```bash
cp .env.server.example .env
chmod 600 .env
nano .env
```

服务器 `.env` 中应设置：

```dotenv
MYSQL_HOST=mysql
TRANSCRIPTION_ENABLED=true
TINGWU_ENABLED=true
```

并填写完整的 MySQL、听悟、R2、LLM 和 SMTP 配置。`MYSQL_PASSWORD` 必须与 Compose 创建 MySQL 用户时使用的密码一致。

安全注意：`docker-compose.server.yml` 当前把 MySQL root 密码写成了示例值，并把宿主机 `3306` 映射到容器。正式部署前应修改 root 密码，并通过防火墙禁止公网访问 3306；如果不需要从宿主机连接数据库，可以删除 MySQL 的 `ports` 配置。

### 7.5 先启动 MySQL 并导入数据库

启动 MySQL：

```bash
docker compose -f docker-compose.server.yml up -d mysql
docker compose -f docker-compose.server.yml ps
```

等待 MySQL 状态变为 `healthy`，然后导入从现有环境导出的备份：

```bash
docker compose -f docker-compose.server.yml exec -T mysql \
  sh -c 'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' \
  < smartsync-backup.sql
```

不要跳过数据库导入。当前应用不能从空库自动创建全部核心表和管理员账号。

### 7.6 构建并启动全部服务

```bash
docker compose -f docker-compose.server.yml up -d --build
docker compose -f docker-compose.server.yml ps
```

当前服务包括：

- `mysql`：MySQL 8.0
- `backend`：FastAPI
- `frontend`：Vue 静态页面
- `nginx`：统一入口

默认访问地址：

```text
http://SERVER_IP:9090
```

验证：

```bash
curl -i http://127.0.0.1:9090/api/health
docker compose -f docker-compose.server.yml logs --tail=100 backend
docker compose -f docker-compose.server.yml logs --tail=100 nginx
```

`/api/realtime/ws` 是 WebSocket 连接，现有 `nginx.server.conf` 已包含 Upgrade 和长连接超时配置。

### 7.7 配置域名和 HTTPS

现有 Docker Nginx 只监听容器 HTTP 端口。生产环境建议在宿主机或负载均衡器上终止 TLS，再代理到 `127.0.0.1:9090`。

宿主机 Nginx 示例：

```nginx
server {
    listen 443 ssl http2;
    server_name meeting.example.com;

    ssl_certificate     /etc/letsencrypt/live/meeting.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/meeting.example.com/privkey.pem;

    client_max_body_size 200m;

    location / {
        proxy_pass http://127.0.0.1:9090;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400s;
    }
}

server {
    listen 80;
    server_name meeting.example.com;
    return 301 https://$host$request_uri;
}
```

证书路径和域名按实际环境修改。配置完成后，从 `https://meeting.example.com` 访问并验证浏览器麦克风权限和实时 WebSocket。

## 8. 数据持久化、备份和升级

### 8.1 Docker 数据卷

服务器 Compose 使用两个命名卷：

- `mysql_data`：数据库数据
- `uploads`：上传的原录音和实时 WAV 录音

执行普通 `docker compose down` 不会删除命名卷。不要在生产环境执行 `docker compose down -v`，除非已经确认要永久删除数据库和录音。

离线上传的本地录音还会保存到 R2。通过 URL 导入的远程音频不会自动复制为本地原文件。

### 8.2 备份数据库

```bash
mkdir -p backups
docker compose -f docker-compose.server.yml exec -T mysql \
  sh -c 'mysqldump -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" --single-transaction --routines --triggers "$MYSQL_DATABASE"' \
  > "backups/smartsync-$(date +%F-%H%M%S).sql"
```

还应定期备份 Docker `uploads` 卷或将其同步到受控存储，并为 R2 Bucket 配置合理的生命周期和备份策略。

### 8.3 更新版本

更新前先备份数据库和录音，然后执行：

```bash
cd /opt/smartsync
git pull
docker compose -f docker-compose.server.yml up -d --build
docker compose -f docker-compose.server.yml ps
curl -i http://127.0.0.1:9090/api/health
```

如新版本包含数据库迁移，必须先审查 SQL，再在备份完成后执行。

### 8.4 停止和重新启动

```bash
docker compose -f docker-compose.server.yml stop
docker compose -f docker-compose.server.yml start
```

彻底移除容器但保留数据卷：

```bash
docker compose -f docker-compose.server.yml down
```

## 9. 常见故障处理

### 9.1 后端无法启动

检查：

```bash
docker compose -f docker-compose.server.yml ps
docker compose -f docker-compose.server.yml logs --tail=200 backend
docker compose -f docker-compose.server.yml logs --tail=200 mysql
```

常见原因：MySQL 未健康、数据库密码不一致、未导入核心表、Python 依赖下载失败或 Dockerfile 固定代理不可访问。

### 9.2 R2 上传返回 401 Unauthorized

重点检查：

- 使用的是 R2 S3 API Token 生成的 Access Key ID 和 Secret Access Key
- Token 未被撤销且具有目标 Bucket 的对象读取和写入权限
- Endpoint 是账户级地址，不包含 Bucket 路径
- `TINGWU_S3_BUCKET` 与实际 Bucket 名称完全一致
- `TINGWU_S3_REGION=auto`

### 9.3 听悟返回 Audio file link invalid

- 保持 `TINGWU_S3_PUBLIC_URL_BASE=` 为空
- 不要把 `r2.dev` 地址提交给听悟
- 确认服务器时间准确，建议启用 NTP
- 确认预签名 URL 有效期不少于任务处理时间
- 检查后端日志中的听悟 `ErrorCode` 和 `ErrorMessage`

### 9.4 缺少 nls 1.1.0

日志出现“缺少阿里云 nls 实时转写 SDK”时，在后端虚拟环境重新安装 `requirements.txt`，或重新构建后端镜像。依赖文件已包含阿里云提供的 `nls-1.1.0` wheel 地址。

### 9.5 实时记录无法使用麦克风

- 本地使用 `localhost` 或 `127.0.0.1`
- 远程服务器必须使用 HTTPS
- 在浏览器地址栏允许麦克风权限
- 检查 `/api/realtime/ticket` 是否返回 200
- 检查 WebSocket 是否成功升级为 101
- 确认代理保留 `Upgrade` 和 `Connection` 请求头

### 9.6 上传返回 413

前端限制为 200 MB，Nginx 也配置了 `client_max_body_size 200m`。若仍返回 413，检查最外层 CDN、负载均衡或宿主机 Nginx 是否也允许 200 MB。

### 9.7 听悟任务为 FAILED

查看后端日志中的轮询快照、`ErrorCode` 和 `ErrorMessage`。常见原因包括录音 URL 无效、格式不支持、听悟权限不足、AppKey 不匹配或预签名 URL 已过期。

### 9.8 AI 分析失败

- 在系统设置中测试当前 LLM 连接
- `LLM_BASE_URL` 应包含兼容接口的 `/v1`
- 确认模型支持 `/chat/completions`
- 检查 API Key、模型名称、网络和上游响应格式

### 9.9 邮件没有发送

- 检查 `SMTP_HOST`、`SMTP_PORT`、`SMTP_USER` 和 `SMTP_PASS`
- 465 端口使用 SMTP SSL，其他端口按 STARTTLS 处理
- 联系人必须填写有效邮箱
- 检查后端日志中的 SMTP 异常

## 10. 生产安全建议

- 轮换所有曾经通过截图、日志或聊天暴露的阿里云和 Cloudflare 凭据。
- 使用随机且足够长的 `SECRET_KEY`、数据库密码和管理员密码。
- `.env` 权限设为 `600`，禁止提交到版本库。
- 不向公网开放 MySQL 3306 和后端 8000，只开放 HTTPS 入口。
- 给 R2 Token 仅授予指定 Bucket 的最小读写权限。
- 给阿里云 RAM 用户仅授予听悟所需权限，不使用主账号长期密钥。
- 定期备份 MySQL、`uploads` 卷和 R2 对象。
- 当前请求日志中可能包含请求体、响应体和短期实时票据，应严格限制日志访问权限。
- 当前后台转写使用 FastAPI 进程内后台任务，不是持久化任务队列；容器在转写中重启会中断正在执行的任务。升级或重启前应等待在途转写完成。

## 11. 当前已知限制

- 仓库缺少从零创建全部核心业务表和首个管理员的完整初始化 SQL。
- `docker-compose.yml` 中的旧 Worker 不可用，服务器应使用 `docker-compose.server.yml`。
- Dockerfile 包含特定开发网络的固定代理，换服务器前必须处理。
- Docker 内置 Nginx 当前只提供 HTTP，远程实时录音需要额外配置 HTTPS。
- 实时转写只保存句段级时间轴，没有保存每个词的时间到数据库。
- 实时翻译、自动章节、听悟会议辅助、听悟自动摘要和文本润色当前未接入。
- 离线转写当前固定使用中文源语言和自动发言人数。
- URL 导入只记录远程音频地址，不保证远程源长期可用。

## 12. 交付验收清单

- [ ] `/api/health` 返回 200
- [ ] 管理员和普通成员均可登录并修改密码
- [ ] 用户之间的会议数据相互隔离
- [ ] MP3/WAV/MP4/M4A 上传不超过 200 MB
- [ ] R2 中能看到新上传对象
- [ ] 离线听悟任务完成并生成发言人逐字稿
- [ ] 原录音可播放，点击句段可定位时间轴
- [ ] HTTPS 页面可获取麦克风权限
- [ ] 实时 WebSocket、临时文本、最终文本和 WAV 保存正常
- [ ] AI 分析可生成摘要、决议和行动项
- [ ] 会议纪要模板可以保存并生成纪要
- [ ] SMTP 邮件能够发送到行动项负责人
- [ ] 我的待办和消息中心数据正常
- [ ] MySQL 和录音备份任务已建立

完成以上检查后，系统即可进入试运行。生产运行期间应重点监控后端错误日志、听悟失败状态、R2 上传错误、磁盘空间和数据库备份结果。
