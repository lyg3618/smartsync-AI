# SmartSync 系统功能与本地部署说明

本文档用于梳理当前代码仓中的系统能力、页面结构、后端接口、数据库要点，以及本地部署方式。

适用范围：当前仓库 `D:\Desktop\AI会议\smartsync`

## 1. 系统概述

SmartSync 是一个面向会议场景的协作系统，围绕“会议录音/转写 → 摘要与决议 → 行动项确认 → 分发 → 跟进”形成闭环。

当前技术栈：

- 前端：Vue 3 + Vite + Pinia + Vue Router + Element Plus
- 后端：FastAPI + aiomysql + Pydantic Settings
- 数据库：MySQL 8.0
- 转写：阿里云 Tingwu（已接入）
- 大模型：通过兼容 OpenAI 的接口生成摘要、行动项、模板纪要
- 通知：SMTP 邮件分发

代码目录：

- 前端：`frontend`
- 后端：`backend`
- 反向代理：`nginx`
- 容器编排：`docker-compose.yml`

## 2. 系统功能总览

### 2.1 登录与权限

当前已实现：

- 用户名密码登录
- JWT 鉴权
- 修改密码
- 基于角色区分管理员与普通成员
- 前端路由登录态保护

对应代码：

- `backend/app/routers/auth.py`
- `frontend/src/views/Login.vue`
- `frontend/src/views/Settings.vue`
- `frontend/src/router/index.js`

### 2.2 会议导入与转写

当前已实现：

- 上传本地会议音频文件
- 粘贴公开可访问的音视频链接
- 创建异步上传/转写任务
- 调用阿里云 Tingwu 执行会议转写
- 轮询任务状态
- 保存会议记录与逐字稿

说明：

- 当前后端上传入口为 `POST /upload`
- Tingwu 方案依赖 `TINGWU_ENABLED=true` 及相关密钥配置
- 本地文件上传给 Tingwu 时，需要 `TINGWU_FILE_URL_BASE` 指向外部可访问的上传文件地址

对应代码：

- `backend/app/routers/upload.py`
- `backend/app/routers/tasks.py`
- `backend/app/services/tingwu.py`
- `frontend/src/views/Dashboard.vue`

### 2.3 会议大厅首页

当前已实现：

- 登录后进入会议大厅首页
- 首页提供上传入口与链接录音导入入口
- 展示会议统计卡片
- 展示最新会议焦点区
- 历史会议以列表形式展示
- 支持历史会议逻辑删除
- 页面风格已统一为当前设计体系

对应代码：

- `frontend/src/views/Dashboard.vue`
- `backend/app/routers/meetings.py`

### 2.4 会议详情工作台

会议详情页是系统核心页面，当前已支持：

- 查看会议基础信息
- 播放录音
- 浏览逐字稿
- 查看和编辑会议摘要
- 查看和编辑会议决议
- 使用 AI 重新分析会议内容
- 编辑行动项并确认入库
- 首次分发行动项
- 分发后再次同步变更
- 使用模板生成会议纪要

当前页面结构已改为 3 个 Tab：

- 模板纪要
- 行动项清单
- 会议摘要与决议

并支持：

- 记住每个会议上次打开的 Tab
- 更清晰的工作台式布局

对应代码：

- `frontend/src/views/MeetingDetail.vue`
- `backend/app/routers/meetings.py`

### 2.5 行动项工作台

当前已实现：

- 新增行动项
- 删除行动项
- 编辑任务内容
- 用弹出文本域编辑较长任务内容
- 选择负责人
- 设置截止日期
- 设置状态
- 拖拽排序
- 按负责人筛选
- 按状态筛选
- 只看待补充项
- 只看“分发后更新”项
- 批量补日期
- 无横向滚动的一行筛选区布局

分发后增强能力：

- 自动标记“已分发后更新”
- 支持筛选变更项
- 支持再次同步变更给负责人

对应代码：

- `frontend/src/views/MeetingDetail.vue`
- `backend/app/routers/meetings.py`

### 2.6 行动项分发与再次同步

当前已实现：

- 首次按负责人聚合行动项并发送邮件
- 邮件中包含会议摘要、核心决议、个人待办任务
- 分发后记录分发时间
- 后续更新行动项时标记为“分发后更新”
- 调用再次同步接口，仅同步已变更项

对应代码：

- `backend/app/routers/meetings.py`

### 2.7 模板纪要与多模板管理

当前已实现：

- 用户维护自己的会议纪要模板
- 导入 `.txt` / `.md` 模板内容
- 新建模板
- 编辑模板
- 删除模板
- 设置默认模板
- 基于选中模板和会议转写内容生成纪要
- 导出模板纪要结果

对应代码：

- `backend/app/routers/templates.py`
- `frontend/src/views/MeetingDetail.vue`

### 2.8 我的待办

当前已实现：

- 聚合当前用户可见行动项
- 按状态过滤
- 标记完成
- 跳转回所属会议详情

对应代码：

- `backend/app/routers/todos.py`
- `frontend/src/views/Todos.vue`

### 2.9 联系人与成员管理

当前已实现：

- 管理员新增成员/联系人
- 自动生成用户名
- 默认初始密码为 `123456`
- 删除联系人
- 区分角色字段

对应代码：

- `backend/app/routers/contacts.py`
- `frontend/src/views/Settings.vue`

### 2.10 系统设置

当前已实现：

- 个人信息维护
- 修改密码
- 通知偏好开关（前端侧）
- LLM 配置项维护（前端侧）
- 管理员管理成员
- 退出登录

对应代码：

- `frontend/src/views/Settings.vue`

## 3. 前端页面与布局说明

当前主要页面：

- 登录页：`frontend/src/views/Login.vue`
- 会议大厅：首页：`frontend/src/views/Dashboard.vue`
- 会议详情：`frontend/src/views/MeetingDetail.vue`
- 我的待办：`frontend/src/views/Todos.vue`
- 系统设置：`frontend/src/views/Settings.vue`

公共布局：

- `frontend/src/components/MainLayout.vue`

当前布局特征：

- 左侧导航栏固定
- 左侧导航栏可折叠
- 折叠按钮为更明显的悬浮把手
- 折叠态支持 tooltip
- 右侧内容区独立滚动
- 全站样式已统一

前端 API 访问方式：

- 默认使用 `VITE_API_BASE_URL`
- 未配置时回退到 `/api`
- 代码位置：`frontend/src/services/api.js`

## 4. 后端接口模块

后端入口：

- `backend/app/main.py`

当前注册的接口模块：

- `/auth`
  - `POST /auth/login`
  - `PUT /auth/password`
- `/upload`
  - `POST /upload`
- `/tasks`
  - `GET /tasks/{task_id}/status`
- `/meetings`
  - `GET /meetings`
  - `GET /meetings/{meeting_id}`
  - `POST /meetings/{meeting_id}/confirm`
  - `POST /meetings/{meeting_id}/dispatch`
  - `POST /meetings/{meeting_id}/resync`
  - `DELETE /meetings/{meeting_id}`
- `/contacts`
  - `GET /contacts`
  - `POST /contacts`
  - `DELETE /contacts/{contact_id}`
- `/todos`
  - `GET /todos`
  - `PATCH /todos/{item_id}`
- `/templates`
  - `GET /templates`
  - `POST /templates`
  - `PUT /templates/{template_id}`
  - `DELETE /templates/{template_id}`
- 健康检查
  - `GET /health`

## 5. 数据库说明

### 5.1 当前代码依赖的核心表

根据现有代码与迁移脚本，当前核心数据对象包括：

- `meetings`
- `transcripts`
- `action_items`
- `contacts`
- `upload_tasks`
- `meeting_minutes_templates`
- `dispatch_logs`
- 兼容旧逻辑时还可能存在 `summaries`

### 5.2 关键字段说明

`meetings` 关键字段：

- `id`
- `name`
- `date`
- `duration_sec`
- `task_count`
- `status`
- `audio_url`
- `summary`
- `decisions`
- `user_id`
- `is_deleted`
- `deleted_at`

`action_items` 关键字段：

- `id`
- `meeting_id`
- `owner_id`
- `owner_name`（当前查询语句已在使用，数据库中应存在）
- `content`
- `due_date`
- `status`
- `updated_after_dispatch`
- `last_dispatched_at`
- `created_at`

`contacts` 关键字段：

- `id`
- `name`
- `email`
- `calendar_addr`
- `user_id`
- `username`
- `hashed_password`
- `role`

`meeting_minutes_templates` 关键字段：

- `id`
- `user_id`
- `name`
- `content`
- `is_default`
- `created_at`
- `updated_at`

### 5.3 当前已提供的迁移脚本

仓库内现有 SQL 迁移脚本：

- `backend/migrate_data_isolation.sql`
- `backend/migrate_soft_delete_meetings.sql`
- `backend/migrate_minutes_templates.sql`
- `backend/migrate_action_item_change_tracking.sql`

这些脚本覆盖的是“增量变更”，不是一份完整的从零建库脚本。

这意味着：

- 如果你已经有旧版库，可以按顺序补迁移
- 如果你是全新部署，还需要先准备基础表结构

## 6. 本地部署前准备

### 6.1 环境要求

推荐环境：

- Node.js 18+
- npm 9+
- Python 3.11+（当前环境里存在 3.13 产物，但建议按常规稳定版本部署）
- MySQL 8.0

可选外部依赖：

- 阿里云 Tingwu 凭据
- 大模型 API 地址与密钥
- SMTP 邮件账号

### 6.2 环境变量

模板文件：

- `.env.example`

后端实际读取位置：

- `backend/.env`

原因：

- `backend/app/config.py` 中 `env_file` 指向的是 `backend/.env`

建议操作：

1. 将根目录 `.env.example` 复制为 `backend/.env`
2. 按实际环境修改 MySQL、LLM、SMTP、Tingwu 配置

最少要关注的变量：

- `MYSQL_HOST`
- `MYSQL_PORT`
- `MYSQL_USER`
- `MYSQL_PASSWORD`
- `MYSQL_DB`
- `SECRET_KEY`
- `LLM_API_KEY`
- `LLM_BASE_URL`
- `LLM_MODEL`
- `SMTP_HOST`
- `SMTP_PORT`
- `SMTP_USER`
- `SMTP_PASS`
- `SMTP_FROM`
- `TINGWU_ENABLED`
- `TINGWU_ACCESS_KEY_ID`
- `TINGWU_ACCESS_KEY_SECRET`
- `TINGWU_APP_KEY`
- `TINGWU_FILE_URL_BASE`

## 7. 本地部署方案一：前后端分开启动

这是当前最稳妥、最适合开发调试的方案。

### 7.1 启动 MySQL

先准备一个名为 `smartsync` 的 MySQL 数据库，字符集建议：

- `utf8mb4`
- `utf8mb4_unicode_ci`

示例：

```sql
CREATE DATABASE smartsync CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

然后创建应用账号，或者直接使用已有账号。

### 7.2 初始化数据库

由于仓库中没有完整基础建表 SQL，请按以下原则准备：

1. 先导入你当前项目使用中的基础表结构
2. 再执行仓库中的增量迁移脚本

建议迁移顺序：

1. `backend/migrate_data_isolation.sql`
2. `backend/migrate_soft_delete_meetings.sql`
3. `backend/migrate_minutes_templates.sql`
4. `backend/migrate_action_item_change_tracking.sql`

注意事项：

- `migrate_data_isolation.sql` 假设已有 `meetings`、`contacts` 等基础表
- `meetings.py` 当前读取 `action_items.owner_name`，请确保该字段在实际库中存在

### 7.3 启动后端

在仓库根目录执行：

```powershell
Copy-Item .env.example backend/.env
```

修改 `backend/.env` 后，安装依赖并启动：

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

启动成功后访问：

- `http://127.0.0.1:8000/health`

### 7.4 启动前端

新开一个终端：

```powershell
cd frontend
npm install
npm run dev
```

默认访问：

- `http://127.0.0.1:5173`

如果前后端分开启动，建议设置：

```powershell
$env:VITE_API_BASE_URL="http://127.0.0.1:8000"
npm run dev
```

或在前端项目中使用 `.env.local`：

```env
VITE_API_BASE_URL=http://127.0.0.1:8000
```

## 8. 本地部署方案二：Docker Compose

仓库中已有：

- `docker-compose.yml`
- `nginx/nginx.conf`

当前 Compose 设计目标是：

- `mysql`：数据库
- `backend`：FastAPI
- `frontend`：静态前端
- `nginx`：统一入口代理

理论访问入口：

- 前端：`http://localhost`
- API：`http://localhost/api/...`

### 8.1 当前 Compose 状态说明

当前 `docker-compose.yml` 并不是“开箱即用”的最终版，至少存在以下问题：

- 定义了 `worker` 服务
- `worker` 启动命令引用 `app.workers.celery_app`
- 但当前仓库中不存在 `backend/app/workers/celery_app.py`
- 同时仓库中也未看到前后端 Dockerfile

因此，当前 Compose 文件更适合作为部署草稿，而不是直接运行的最终方案。

### 8.2 如果要继续走容器化

建议先补齐以下内容后再启用：

- 后端 `Dockerfile`
- 前端 `Dockerfile`
- 是否还需要 `worker` 服务的明确实现
- 基础建表 SQL 或初始化脚本
- 上传目录与公开访问路径映射

## 9. 运行链路说明

系统的典型业务流如下：

1. 用户登录系统
2. 进入会议大厅
3. 上传录音文件或粘贴录音链接
4. 后端创建转写任务并调用 Tingwu
5. 任务完成后写入会议、逐字稿、初始摘要
6. 用户进入会议详情页
7. 用户修订摘要、决议、行动项
8. 用户可切换到模板纪要 Tab 生成标准纪要
9. 用户首次分发行动项
10. 后续如果有补充修改，可标记“分发后更新”并再次同步
11. 负责人在“我的待办”中查看和完成任务

## 10. 已知注意事项

### 10.1 数据库初始化并非完整闭环

当前仓库只有增量迁移 SQL，没有完整基础建库脚本。

建议：

- 在交付前补一份 `init.sql`
- 或增加 Alembic 全量建表能力

### 10.2 容器部署尚未闭环

当前 `docker-compose.yml` 中的 `worker` 依赖不存在的 Celery 入口，不能直接视为可用生产方案。

### 10.3 Tingwu 对公网地址有要求

如果使用本地上传文件交给 Tingwu 识别，`TINGWU_FILE_URL_BASE` 必须是公网可访问地址，不能只填本机磁盘路径。

### 10.4 邮件分发依赖 SMTP

如果未配置：

- `SMTP_USER`
- `SMTP_PASS`

则行动项邮件分发能力无法完整发挥。

## 11. 推荐交付物补充

如果后续需要把系统整理到可交付、可重装、可维护状态，建议再补以下文件：

- `backend/init.sql`：完整基础建表脚本
- `backend/Dockerfile`
- `frontend/Dockerfile`
- `docs/api.md`：接口文档
- `docs/deploy.md`：正式部署文档
- `docs/change-log.md`：变更记录

## 12. 与当前代码对应的重点文件

前端：

- `frontend/src/components/MainLayout.vue`
- `frontend/src/views/Login.vue`
- `frontend/src/views/Dashboard.vue`
- `frontend/src/views/MeetingDetail.vue`
- `frontend/src/views/Todos.vue`
- `frontend/src/views/Settings.vue`
- `frontend/src/services/api.js`

后端：

- `backend/app/main.py`
- `backend/app/config.py`
- `backend/app/database.py`
- `backend/app/models.py`
- `backend/app/routers/auth.py`
- `backend/app/routers/upload.py`
- `backend/app/routers/tasks.py`
- `backend/app/routers/meetings.py`
- `backend/app/routers/todos.py`
- `backend/app/routers/contacts.py`
- `backend/app/routers/templates.py`

数据库迁移：

- `backend/migrate_data_isolation.sql`
- `backend/migrate_soft_delete_meetings.sql`
- `backend/migrate_minutes_templates.sql`
- `backend/migrate_action_item_change_tracking.sql`

---

如果只按当前仓库落地本地开发，推荐优先采用“方案一：MySQL + 后端 + 前端分别启动”。这是当前最清晰、风险最低的方式。
